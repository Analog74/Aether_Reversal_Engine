# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.whl
.venv/
venv/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# -------------------------------------------------------
# targets/ — track metadata only; ignore all generated,
# binary, and machine-local content per target
# -------------------------------------------------------

# Per-target generated/binary subdirs (gitignored everywhere under targets/)
targets/*/decompiled/
targets/*/decompiled_real/
targets/*/build/
targets/*/cache/
targets/*/exec/
targets/*/bundle/
targets/*/binary/
targets/*/ghidra_projects/
targets/*/.venv/
targets/*/tools/.venv/

# Per-target extracted asset content — all of assets/ is local-only
# (includes legacy layouts like assets/images/, assets/strings/, etc.)
targets/*/assets/**

# Root-level source binaries (multi-GB, machine-local)
SourceFiles/
ghidra_projects/
extracted/

# Compiled / generated outputs
*.log
ghidra.log
*.jsonl
*.dylib
*.so
*.dll
*.o
*.a
*.zst

# macOS
.DS_Store
**/.DS_Store
