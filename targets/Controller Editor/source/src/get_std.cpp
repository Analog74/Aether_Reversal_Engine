#include <ni/controller_editor/get_std.hpp>
#include "../../../../dynamic_headers/ni_common_types.h"
namespace std::locale fmt::v9::detail::locale_ref {

void get<std::locale>() const {
unsigned long long *in_RSI;
  locale *in_RDI;
  
  if ((locale *)*in_RSI == (locale *)0x0) {
    // std code
  }
  else {
    // std code
  }
  return;
}
}
