#pragma once
namespace undefined __thiscall rethrow(clone_impl<boost {
class exception_detail {
public:

    // +-98 more methods reconstructed
};
}
eption_detail * this);
    virtual auto bad_exception_>(exception_detail * this);
    // +-98 more methods reconstructed
};
}
;
    virtual auto bad_exception_>>(shared_ptr * param_1, clone_impl * param_2, shared_count * param_3);
    // +-98 more methods reconstructed
};
}
